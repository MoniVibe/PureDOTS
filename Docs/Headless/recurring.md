# recurring

- Rebuild pins must exist in all requested repos; if a PureDOTS-only branch is used, create matching refs in space4x/godgame (or pin to origin/main) or the PS builder will fail checkout with pathspec errors.
