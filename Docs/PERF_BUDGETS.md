# Perf Budgets (Tombstone)

Performance budgets and scale gates are canonicalized in:
- `Docs/PERF_GATES.md`

Use `PERF_GATES.md` for thresholds, pass/fail policy, telemetry budget discipline, and budget update rules.
